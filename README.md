# Arz Store — Vercel Ready

## Quick Start
```bash
npm i
npm run dev
```

## Build (for Vercel)
```bash
npm run build
```
- Deploy on Vercel:
  1. Create a new project on Vercel and **Import** this folder.
  2. Framework preset: **Vite**.
  3. Build Command: `vite build`
  4. Output Directory: `dist`
  5. Install command: `npm i`

The WhatsApp number is in `src/App.jsx` (`whatsappLink`). Replace it with your real number.
